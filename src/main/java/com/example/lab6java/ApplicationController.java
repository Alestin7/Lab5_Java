package com.example.lab6java;import javafx.fxml.FXML;import javafx.scene.control.Alert;import javafx.scene.control.TextField;import javafx.scene.image.Image;import javafx.scene.image.ImageView;import javafx.scene.layout.HBox;import java.io.FileWriter;import java.io.IOException;import java.util.ArrayList;import java.util.Date;public class ApplicationController {    @FXML    private HBox hBox = new HBox();    @FXML    private ImageView img = new ImageView();    @FXML    protected void Getdata() throws IOException {        Cat cat = new Cat();        cat = run().get(0);        String url = cat.getUrl();        boolean backgroundLoading = true;// The image is being loaded in the background        Image image = new Image(url, backgroundLoading);        img.setImage(image);    }    protected boolean checkForNumber(String str) {        Integer number = 0;        try {            number = Integer.valueOf(str);            if (number != 0)                return true;            else                return false;        } catch (Exception ex) {            return false;        }    }    public static ArrayList<Cat> run() throws IOException {        Cats cats = new Cats();        try {            cats.loadURLOnNewThread("https://thatcopy.pw/catapi/rest/");        } catch (Exception exception) {            FileWriter fw = new FileWriter(String.valueOf((new Date().getTime() / 1000)), true);            fw.write(exception.getMessage());        }        return cats.getCatList();    }}